name: MLOps Pipeline

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    services:
      qdrant:
        image: qdrant/qdrant:latest
        ports:
          - 6333:6333

    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Set up Python 3.12
      uses: actions/setup-python@v5
      with:
        python-version: "3.12"
        cache: 'pip'

    - name: Install Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install ruff pytest
        pip install -r requirements.txt

    - name: Lint with Ruff
      run: |
        ruff check . --fix

    - name: Wait for Qdrant Service
      run: |
        until curl -s http://localhost:6333/healthz; do
          echo "Waiting for Qdrant..."
          sleep 2
        done

    - name: Ingest Test Data (CI)
      env:
        GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
        QDRANT_URL: "http://localhost:6333"
        PYTHONPATH: .
      run: |
        python -c "
        from src.rag import engine;
        from langchain.schema import Document;
        docs = [
            Document(page_content='PDF test doc', metadata={'source': 'test'})
        ];
        engine.vector_store.add_documents(docs);
        print('✅ Test data ingested - DB Connection OK');
        "

    - name: Component Initialization Check
      env:
        GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
        LANGFUSE_PUBLIC_KEY: ${{ secrets.LANGFUSE_PUBLIC_KEY }}
        LANGFUSE_SECRET_KEY: ${{ secrets.LANGFUSE_SECRET_KEY }}
        QDRANT_URL: "http://localhost:6333"
        PYTHONPATH: .
      run: |
        python -c "from src.rag import get_rag_chain; print('✅ RAG Chain initialized successfully')"
